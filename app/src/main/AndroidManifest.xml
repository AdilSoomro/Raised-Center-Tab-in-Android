<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.booleanbites.tabsample"
    >



    <application
        android:icon="@drawable/icon"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".legacy.AwesomeActivity"
            android:theme="@style/app_legacy_theme">

        </activity>
        <activity android:name=".legacy.HomeActivity" />
        <activity android:name=".legacy.SearchActivity" />
        <activity android:name=".legacy.CameraActivity" />

    </application>

</manifest> 